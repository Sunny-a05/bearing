# raw/ — immutable sources

Fully-ingested source material. Files land here through the dock and are **never
modified**: every factual claim in `wiki/` should trace back to something in this
folder.

To add something, drop it in `os/dock/inbox/` and run the pipeline — do not copy
files here by hand, or dedup and the log will not know about them.
